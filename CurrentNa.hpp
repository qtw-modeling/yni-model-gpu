#include "common.hpp"

real alpha_m(real V);
real beta_m(real V);

real alpha_h(real V);
real beta_h(real V);

real m_inf(real V);
real h_inf(real V);

real CurrentNa(real V, real m, /* real n,*/ real h);